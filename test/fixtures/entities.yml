e1v1:
  name: NotUsed
  version: project1v1
  root: "{id:number}"
e2v1:
  name: User
  version: project1v1
  root: "{id:string, first_name:string, last_name:string, role:number}"
e3v1:
  name: Error
  version: project1v1
  root: "{code:number, message:string}"
e4v1:
  name: Record
  version: project1v1
  root: "{id:number, quantity:number, is_available: boolean, name:string}"

e1v2:
  name: NotUsed
  version: project1v2
  root: "{id:number}"
e2v2:
  name: User
  version: project1v2
  root: "{id:string, first_name:string, last_name:string, role:number}"
e3v2:
  name: Error
  version: project1v2
  root: "{code:number, message:string}"
e4v2:
  name: Record
  version: project1v2
  root: "{id:number, quantity:number, is_available: boolean, name:string}"
