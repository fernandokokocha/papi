<%# locals: (blocks:, base:, landed: nil) %>
<div id="entities" class="flex flex-col gap-4">
  <% blocks.entities.each do |block| %>
    <%= render "entities/form_card", blocks: blocks, block: block, base: base, landed: landed %>
  <% end %>
</div>
