<% output_diff = endpoint.parsed_output.to_diff(:no_change) %>
<% output_error_diff = endpoint.parsed_output_error.to_diff(:no_change) %>
<% responses_diff = DiffResponses::FromResponses.new(endpoint.responses, []) %>
<div class="grid grid-cols-2 gap-2">
  <div>
    <div class="border border-red-200 rounded-lg">
      <div class="sticky top-0 z-10 rounded-t-lg bg-gray-900 text-white px-4 py-3 text-sm font-mono line-through opacity-60">
        <%= endpoint.name %>
      </div>
      <div class="bg-red-50 border-t border-red-200 px-3 py-1.5 text-xs font-semibold text-black uppercase tracking-wide">Note</div>
      <div class="px-3 py-2 bg-red-50 border-b border-red-200 text-sm text-gray-700"><%= endpoint.note %></div>
      <div class="bg-red-50 border-t border-red-200 px-3 py-1.5 text-xs font-semibold text-black uppercase tracking-wide">Responses</div>
      <div class="pl-2 py-2 bg-red-50 border-b border-red-200"><%= render "specs/responses", diff: responses_diff.before %></div>
      <div class="bg-red-50 border-t border-red-200 px-3 py-1.5 text-xs font-semibold text-black uppercase tracking-wide">Output</div>
      <div class="pl-2 py-2 bg-red-50 border-b border-red-200"><%= render "specs/json", diff: output_diff %></div>
      <div class="bg-red-50 border-t border-red-200 px-3 py-1.5 text-xs font-semibold text-black uppercase tracking-wide">Output for Errors</div>
      <div class="pl-2 py-2 bg-red-50 border-b border-red-200"><%= render "specs/json", diff: output_error_diff %></div>
    </div>
  </div>
  <div></div>
</div>
